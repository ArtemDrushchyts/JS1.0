describe('Сумма', function() {
	it('Значение равно true?', function() {
		assert.typeOf(sum(2,2), 'boolean');
	});

	it('Переменная num должна быть равна 5?', function() {
		assert.equal(num, 5);
	});

	it('Что вернет функция?', function() {
		assert
	})
});
